type IProps = {
  genre: {
    id: string;
    imageBanner: string;
    genre: string;
  };
};

type IViewProps = {
  genre: {
    id: string;
    imageBanner: string;
    genre: string;
  };
};

export { IProps, IViewProps };
